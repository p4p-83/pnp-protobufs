syntax = "proto3";

package pnp.v1;

message Message {
  Tags tag = 1;
  oneof payload { Deltas deltas = 2; }

  enum Tags {
    INVALID = 0;
    HEARTBEAT = 1;
    TARGET_DELTAS = 2;
    MOVED_DELTAS = 3;
  }

  message Deltas {
    sint32 x = 1;
    sint32 y = 2;
  }
}
